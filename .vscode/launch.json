{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/client",
      "terminal": "external",
      "args": ["localhost", "13"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "make"
    }
  ]
}
