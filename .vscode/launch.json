{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/client",
      "terminal": "external",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "make"
    }
  ]
}
